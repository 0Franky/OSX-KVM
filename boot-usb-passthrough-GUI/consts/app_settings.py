font_name = "Roboto"
font_size = 12
font_size_small = 10

# generic
metrix = "px"

# rounding
border_radius_value = 10
border_radius = f"{border_radius_value}{metrix}"
border_radius_cb = f"{border_radius_value * 0.35}{metrix}"

# spacing
padding_value = 20
padding = f"{padding_value}{metrix}"
padding_small = f"{int(padding_value * 0.5)}{metrix}"

margin_value = 15
margin = f"{margin_value}{metrix}"